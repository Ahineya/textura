import "../src/lib/styles.scss";

const preview = {
  parameters: {
    backgrounds: {
      default: 'default',
      values: [
        {
          name: 'default',
          value: '#2D2B2D',
        },
      ],
    },
  },
};

export default preview;
